# Pages
We are now using `11ty`, amazing tool, worth checking out

## Build Locally

```sh
npm install
npm start       # dev server at http://localhost:8080 with live reload
npm run build   # one-off build to _site/
```
